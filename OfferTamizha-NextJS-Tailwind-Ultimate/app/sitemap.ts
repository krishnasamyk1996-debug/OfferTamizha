import type { MetadataRoute } from 'next'
export default function sitemap(): MetadataRoute.Sitemap { const base=process.env.NEXT_PUBLIC_SITE_URL||'https://offertamizha.com'; const now=new Date(); return [{url:base,lastModified:now,changeFrequency:'hourly',priority:1},{url:`${base}/privacy`,lastModified:now,changeFrequency:'monthly',priority:.3},{url:`${base}/affiliate-disclosure`,lastModified:now,changeFrequency:'monthly',priority:.3},{url:`${base}/terms`,lastModified:now,changeFrequency:'monthly',priority:.3},{url:`${base}/contact`,lastModified:now,changeFrequency:'monthly',priority:.4}] }
